# Conventions

- Keep the registry as the single source of truth.
- Keep CLI, REST, MCP, and SDK adapters thin.
- Add or update tests with behavior changes.
- Do not add consuming-project-specific concepts to the generic package.
- Keep files short and split by responsibility.

